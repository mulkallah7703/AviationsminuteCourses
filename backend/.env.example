# Optional Express overrides — copy to backend/.env
PORT=4000
NODE_ENV=development

# DATABASE_URL and JWT_* belong in the repo root .env (Prisma + API read from there).

# Optional: restrict browser origin in production (comma-separated)
# CORS_ORIGIN=https://your-app.vercel.app
